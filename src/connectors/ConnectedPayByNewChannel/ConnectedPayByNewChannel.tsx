import { FC, ReactElement } from 'react';

import PayByNewChannel from '../../containers/PayByNewChannel/PayByNewChannel';

const ConnectedPayByNewChannel: FC = (): ReactElement => (
    <PayByNewChannel />
);

export default ConnectedPayByNewChannel;
